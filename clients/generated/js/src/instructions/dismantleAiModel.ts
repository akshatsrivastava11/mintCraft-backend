/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const DISMANTLE_AI_MODEL_DISCRIMINATOR = new Uint8Array([
  118, 72, 195, 47, 67, 125, 171, 254,
]);

export function getDismantleAiModelDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISMANTLE_AI_MODEL_DISCRIMINATOR
  );
}

export type DismantleAiModelInstruction<
  TProgram extends string = typeof MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountAiModel extends string | AccountMeta<string> = string,
  TAccountGlobalState extends string | AccountMeta<string> = string,
  TAccountUserConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountAiModel extends string
        ? WritableAccount<TAccountAiModel>
        : TAccountAiModel,
      TAccountGlobalState extends string
        ? ReadonlyAccount<TAccountGlobalState>
        : TAccountGlobalState,
      TAccountUserConfig extends string
        ? WritableAccount<TAccountUserConfig>
        : TAccountUserConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DismantleAiModelInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type DismantleAiModelInstructionDataArgs = { name: string };

export function getDismantleAiModelInstructionDataEncoder(): Encoder<DismantleAiModelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DISMANTLE_AI_MODEL_DISCRIMINATOR })
  );
}

export function getDismantleAiModelInstructionDataDecoder(): Decoder<DismantleAiModelInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDismantleAiModelInstructionDataCodec(): Codec<
  DismantleAiModelInstructionDataArgs,
  DismantleAiModelInstructionData
> {
  return combineCodec(
    getDismantleAiModelInstructionDataEncoder(),
    getDismantleAiModelInstructionDataDecoder()
  );
}

export type DismantleAiModelAsyncInput<
  TAccountSigner extends string = string,
  TAccountAiModel extends string = string,
  TAccountGlobalState extends string = string,
  TAccountUserConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  aiModel?: Address<TAccountAiModel>;
  globalState?: Address<TAccountGlobalState>;
  userConfig?: Address<TAccountUserConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: DismantleAiModelInstructionDataArgs['name'];
};

export async function getDismantleAiModelInstructionAsync<
  TAccountSigner extends string,
  TAccountAiModel extends string,
  TAccountGlobalState extends string,
  TAccountUserConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends
    Address = typeof MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DismantleAiModelAsyncInput<
    TAccountSigner,
    TAccountAiModel,
    TAccountGlobalState,
    TAccountUserConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DismantleAiModelInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountAiModel,
    TAccountGlobalState,
    TAccountUserConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    aiModel: { value: input.aiModel ?? null, isWritable: true },
    globalState: { value: input.globalState ?? null, isWritable: false },
    userConfig: { value: input.userConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.globalState.value) {
    accounts.globalState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            103, 108, 111, 98, 97, 108, 65, 105, 83, 116, 97, 116, 101,
          ])
        ),
      ],
    });
  }
  if (!accounts.aiModel.value) {
    accounts.aiModel.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 105])),
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()).encode(
          expectSome(args.name)
        ),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
        getAddressEncoder().encode(expectAddress(accounts.globalState.value)),
      ],
    });
  }
  if (!accounts.userConfig.value) {
    accounts.userConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([117, 115, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.aiModel),
      getAccountMeta(accounts.globalState),
      getAccountMeta(accounts.userConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getDismantleAiModelInstructionDataEncoder().encode(
      args as DismantleAiModelInstructionDataArgs
    ),
  } as DismantleAiModelInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountAiModel,
    TAccountGlobalState,
    TAccountUserConfig,
    TAccountSystemProgram
  >;

  return instruction;
}

export type DismantleAiModelInput<
  TAccountSigner extends string = string,
  TAccountAiModel extends string = string,
  TAccountGlobalState extends string = string,
  TAccountUserConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  aiModel: Address<TAccountAiModel>;
  globalState: Address<TAccountGlobalState>;
  userConfig: Address<TAccountUserConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: DismantleAiModelInstructionDataArgs['name'];
};

export function getDismantleAiModelInstruction<
  TAccountSigner extends string,
  TAccountAiModel extends string,
  TAccountGlobalState extends string,
  TAccountUserConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends
    Address = typeof MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DismantleAiModelInput<
    TAccountSigner,
    TAccountAiModel,
    TAccountGlobalState,
    TAccountUserConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): DismantleAiModelInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountAiModel,
  TAccountGlobalState,
  TAccountUserConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    aiModel: { value: input.aiModel ?? null, isWritable: true },
    globalState: { value: input.globalState ?? null, isWritable: false },
    userConfig: { value: input.userConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.aiModel),
      getAccountMeta(accounts.globalState),
      getAccountMeta(accounts.userConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getDismantleAiModelInstructionDataEncoder().encode(
      args as DismantleAiModelInstructionDataArgs
    ),
  } as DismantleAiModelInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountAiModel,
    TAccountGlobalState,
    TAccountUserConfig,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedDismantleAiModelInstruction<
  TProgram extends string = typeof MINT_CRAFT_MODEL_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    aiModel: TAccountMetas[1];
    globalState: TAccountMetas[2];
    userConfig: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: DismantleAiModelInstructionData;
};

export function parseDismantleAiModelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDismantleAiModelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      aiModel: getNextAccount(),
      globalState: getNextAccount(),
      userConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDismantleAiModelInstructionDataDecoder().decode(instruction.data),
  };
}
